import Login from './Login';
import {Routes} from '../../app-config/constants'

const LoginConfig = {
  name: Routes.LOGIN,
  component: Login,
  options: {
    headerShown: false
  },
};

export default LoginConfig;
